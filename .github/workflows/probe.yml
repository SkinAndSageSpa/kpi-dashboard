name: Probe Mangomint Reports

on:
  workflow_dispatch:
    inputs:
      account:
        description: 'Which account to probe'
        required: false
        default: 'both'
        type: choice
        options: [both, skinsage, waxon]

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SCREENSHOT_DIR: /tmp/kpi-probe-screenshots
      SKINSAGE_MANGOMINT_COOKIES: ${{ secrets.SKINSAGE_MANGOMINT_COOKIES }}
      WAXON_MANGOMINT_COOKIES: ${{ secrets.WAXON_MANGOMINT_COOKIES }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run probe
        run: node src/probe.js

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: probe-screenshots
          path: /tmp/kpi-probe-screenshots/
          retention-days: 7
